<UserControl x:Class="Intems.LightPlayer.GUI.Controls.DiscoverControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ViewModels="clr-namespace:Intems.LightPlayer.GUI.ViewModels"
             mc:Ignorable="d" >
    <Grid>
        <StackPanel Orientation="Horizontal">
            <TextBox MinHeight="25" MaxHeight="30" MinWidth="150"
                     HorizontalAlignment="Stretch"
                     Text="{Binding FileName}"/>
            <Button MinHeight="25" MaxHeight="30"
                    Content="Load"
                    Command="{Binding Command}" 
                    CommandParameter="{Binding Path=., RelativeSource={RelativeSource AncestorType={x:Type ViewModels:MainViewModel}}}"/>
        </StackPanel>
    </Grid>
</UserControl>
